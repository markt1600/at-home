# Optional PUBLIC ElevenLabs conversational agent identifier, never an API key.
ELEVENLABS_AGENT_ID=
# Secret, server-side only: Vercel function generates spoken audio.
ELEVENLABS_API_KEY=
# Optional. Uses the agent's configured voice when available.
ELEVENLABS_VOICE_ID=
